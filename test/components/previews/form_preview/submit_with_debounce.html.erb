<%= form_with url: main_app.slow_action_path, html: { autocomplete: "off" }, data: { controller: "form" } do |form| %>
  <%= form.search_field :filter_tasks, placeholder: "Filter tasks..", class: "input", data: { action: "form#debouncedSubmit" } %>
<% end %>
