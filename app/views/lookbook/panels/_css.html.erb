<% if defined?(filename) %>
  <%= lookbook_render :code, language: :css, line_numbers: true, full_height: true do %><%== File.read(filename) %><% end %>
<% else %>
  <div class="lookbook-panel bg-white">
    <em class="opacity-50">CSS is not required.</em>
  </div>
<% end %>